services:
  backend:
    build: .
    container_name: academy-backend
    restart: unless-stopped
    env_file:
      - .env
    expose:
      - "3001"
    volumes:
      - /opt/docker/academy-backend/data:/app/server/data
    networks:
      - npm_network

networks:
  npm_network:
    external: true
